class Solution {
    public int reverse(int x) {
        int rev=0;
        while(x!=0){
            int digit=x%10;
            int prev=rev;
            rev=rev*10+digit;
            if((rev-digit)/10!=prev){
                return 0;
            }
            x/=10;
        }
        return rev;
    }
}
